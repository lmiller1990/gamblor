<div class="game_show">

  <div>
    <%= @game.date.strftime("%Y/%m/%d") %>
  </div>

  <% if @game.winner_id && @game.loser_id %>

    <span class="winner"><%= @game.winner.name %></span> vs <%= @game.loser.name %>

  <% else %>

    <%= @game.blue_side_team.name %> vs <%= @game.red_side_team.name %>

  <% end %>
</div>

<div>
  First blood: <%= @first_blood_team %> (<%= @game.player_to_get_first('blood').name %>)
</div>

<div>
  First turret: <%= @first_turret_team %> (<%= @game.player_to_get_first('turret').name %>)
</div>

<div>
  First dragon: <%= @first_dragon_team %> (<%= @game.player_to_get_first('dragon').name %>)
</div>

<div>
  First baron: <%= @first_baron_team %> (<%= @game.player_to_get_first('baron').name %>)
</div>

<%= link_to 'Edit', edit_game_path(@game), method: :get %>
